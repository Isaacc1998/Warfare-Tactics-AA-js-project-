let character = document.addEventListener('click', e => {
    renderOptions();
});
let hover = document.addEventListener('mouseover', );
//attack button
let attackButton = document.addEventListener('click', );
//click enemy
let attack = document.addEventListener('click', );

//click move button
let moveButton = document.addEventListener('click', );
//click square
let move = document.addEventListener('click', );
//open pause
let pause = document.addEventListener('click', );
//restart battle
let restart = document.addEventListener('click', );
//return to menu
let menu = document.addEventListener('click', );





